/*import React from "react";
import Mydata from "../Mydata/Mydata";


const Verify = ({ url }) => {
  const userId = localStorage.getItem("userId"); // saved at login

  return (
    <div>
      <Mydata url={url} userId={userId} />
    </div>
  );
};

export default Verify;*/
